name: Deploy to GitHub Pages

on:
  push:
    branches:
      - main # или ваш основной branch

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout 🛎️
        uses: actions/checkout@v3

      - name: Setup Node.js 🖥️
        uses: actions/setup-node@v3
        with:
          node-version: '18' # или другая версия Node.js, которую вы используете

      - name: Install dependencies 🔧
        run: npm ci # или yarn install, если вы используете Yarn

      - name: Build project 🏗️
        run: npm run build # или yarn build

      - name: Export project for static hosting 🌐
        run: npm run export # или yarn export
        # Убедитесь, что в next.config.js добавлен параметр trailingSlash: true

      - name: Upload artifact 📦
        uses: actions/upload-pages-artifact@v2
        with:
          path: ./out # папка с экспортированными файлами (по умолчанию .next)

      - name: Deploy to GitHub Pages 🚀
        uses: actions/deploy-pages@v4